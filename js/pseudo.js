let clubberAge = prompt("Enter Your Age");
let vipAge = 35;
if (clubberAge < vipAge) {
  alert("Not Up to VIP Age");
} else if (clubberAge >= vipAge) {
  alert("Welcome to the VIP section");
} else {
  alert("invalid: Enter a number");
}
